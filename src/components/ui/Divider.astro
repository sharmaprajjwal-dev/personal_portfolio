---
interface Props {
  withNode?: boolean;
}

const { withNode = false } = Astro.props;
---

<div class="relative h-px w-full bg-white/10" role="presentation">
  {withNode && (
    <span class="absolute left-0 top-1/2 -translate-y-1/2 w-1.5 h-1.5 rounded-full bg-accent"></span>
  )}
</div>
